// @ts-nocheck
import {Routes} from "@angular/router";

const routes: Routes = [
    {
        path: '',
        title: `Map Components`,
        children: [
        ]
    }
]

export default routes;
